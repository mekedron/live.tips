{
  "name": "livetips-stage",
  "version": "1.0.0",
  "private": true,
  "description": "live.tips tip-jar stage library (3D three.js + 2D canvas renderers behind one bridge)",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
