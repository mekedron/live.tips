# .well-known/ — placeholder

Reserved for a later phase: `apple-app-site-association` and
`assetlinks.json` (universal links / app links for tip.live.tips) will live
here. Keeping the directory in the deploy now means adding those files later
is a content change, not a hosting-config change.
