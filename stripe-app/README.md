# live.tips Setup Assistant (Stripe App) — not started

Phase-2 companion app for the Stripe App Marketplace: a dashboard-only UI
extension that creates the tip jar from inside the artist's Stripe dashboard
and shrinks the mobile app's key to read-only permissions.

See [`docs/stripe-app-plan.md`](../docs/stripe-app-plan.md) for the verified
findings and the plan. Scaffold with `stripe apps create` when phase 2 starts.
